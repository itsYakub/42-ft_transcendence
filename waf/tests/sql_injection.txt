# injection in-form
curl -sk -X POST https://transcendence.nip.io:3000/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  --data "username=admin&password=1' OR '1'='1" -o /dev/null -w "%{http_code}\n"

# basic
curl -sk "https://transcendence.nip.io:3000/?q=<script>alert(1)</script>" -o /dev/null -w "%{http_code}\n"

# in-comment
curl -sk "https://transcendence.nip.io:3000/?search=test'/*" -o /dev/null -w "%{http_code}\n"
