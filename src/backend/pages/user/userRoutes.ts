import { FastifyInstance, FastifyRequest, FastifyReply } from 'fastify';
import { DatabaseSync } from "node:sqlite";

export function userRoutes(fastify: FastifyInstance, db: DatabaseSync): void {

}
